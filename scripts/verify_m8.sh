#!/usr/bin/env bash
# M8 end-to-end verify — four-layer context engine on real repo.
# Kill-gate: still context-starved -> add tool-use-at-retrieval before shipping v2.
set -euo pipefail
echo "TODO: M8 verify — joint query across AST+git+runtime+embeddings on non-toy repo"
exit 1
