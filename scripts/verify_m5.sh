#!/usr/bin/env bash
# M5 end-to-end verify — firejail sandbox + diff approval + merge-on-accept.
# Kill-gate: verification false-pass rate > 15% on 20-intent suite -> STOP.
set -euo pipefail
echo "TODO: M5 verify — intent -> isolated worktree -> approval -> merged commit"
exit 1
