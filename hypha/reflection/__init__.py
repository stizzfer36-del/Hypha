"""Self-improvement layer — run-log + reflection + variants + canary.

The canary guard is the non-negotiable defense against reward-hacking in a
gradient-free prompt search. Do not enable reflection without it.
"""
